# Base URL of the backend API
# If empty or not set, the frontend will default to relative URLs (e.g. /api/v1/...) 
# which is the recommended configuration when served via the Nginx proxy in Docker.
# For local development with Vite dev server, you can set this to http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
